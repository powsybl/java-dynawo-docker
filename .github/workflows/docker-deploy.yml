name: Docker Deploy

on:
  workflow_dispatch:

jobs:
  run-deploy:
    uses: powsybl/github-ci/.github/workflows/manual-dockerdeploy-base-docker-image-generic.yml@c49157c2139394b2ecfde6d99c827ac39d014070
    with:
      dockerImage: docker.io/powsybl/java-dynawo
      dockerUsername: powsyblci
    secrets:
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
